import sys
print (f"Name of the script : {sys.argv[0]=}")              # eturns name of the script (arvgTest.py)
print (f"Arguments of the script    : {sys.argv[1:]=}")     # returns any arguments entered in command line